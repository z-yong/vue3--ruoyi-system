export interface SettingType extends Object {
  theme: string;
  sideTheme: string;
  showSettings: boolean;
  tagsView: boolean;
  fixedHeader: boolean;
  sidebarLogo: boolean;
  menuDrag: boolean;
  menuWidth: number;
}
